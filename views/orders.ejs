<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Заказы</title>
</head>
<body>
    <h1>Заказы</h1>

    <nav>
        <a href="/products">Каталог товаров</a>
        <a href="/logout">Выйти</a>
    </nav>

    <hr>

    <% if (orders.length === 0) { %>
        <p>Заказы пока не добавлены.</p>
    <% } else { %>
        <% orders.forEach(order => { %>
            <div style="border: 1px solid #ccc; padding: 15px; margin-bottom: 15px;">
                <h2>Заказ №<%= order.orderNumber %></h2>

                <p><b>Дата заказа:</b> <%= order.orderDate %></p>
                <p><b>Дата доставки:</b> <%= order.deliveryDate %></p>
                <p><b>ID пункта выдачи:</b> <%= order.pickupPointId %></p>
                <p><b>ID клиента:</b> <%= order.clientId %></p>
                <p><b>Код получения:</b> <%= order.pickupCode %></p>
                <p><b>Статус:</b> <%= order.status %></p>
            </div>
        <% }) %>
    <% } %>
</body>
</html>